# Scraping and Translating

Simply install dependencies using `pip install -r requirements.txt`. Then use the `scrape-and-translate.sh` command to scrape and immediately translate Dutch tweets to English.

